export interface ButtonProps {
  $back?: string,
  $color?: string,
  $size?: string,
  $padding?: string,
  $radius?: string,
  $border?: string,
}

export type CategoryProps = {
  $numericCalor: 1 | 2 | 3 | 4,
}
